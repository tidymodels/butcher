# Get path to model object example.

butcher comes bundled with some example files in its `inst/extdata`
directory. This function was copied from readxl and placed here to make
the instantiated model objects easy to access.

## Usage

``` r
butcher_example(path = NULL)
```

## Arguments

- path:

  Name of file. If `NULL`, the example files will be listed.
